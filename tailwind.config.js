/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{html,ts}" // Quét các file HTML và TypeScript trong src
  ],
  theme: {
    extend: {}, // Tùy chỉnh thêm nếu cần
  },
  plugins: [],
};
